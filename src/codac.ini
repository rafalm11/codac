[DEFAULT]
countryFilter = Netherlands


[LOGS]
logFormat = %%(asctime)s - %%(name)s - %%(levelname)s - %%(message)s
logsFile = logs/test.log
maxBytes = 10000
backupCount = 5

[SPARK]
sparkUrl = local
outputFolderName = client_data

[LOGIC]
renameMap = {"id":"client_identifier","btc_a":"bitcoin_address","cc_t":"credit_card_type"}
